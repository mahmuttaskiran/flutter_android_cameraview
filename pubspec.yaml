name: flutter_camera_view
description: A new flutter plugin project.
version: 0.0.1
author: mahmutttaskiran
homepage:

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  plugin:
    androidPackage: com.mahmuttaskiran.cameraview.flutter_camera_view
    pluginClass: FlutterCameraViewPlugin